
services:
  book-arbitrage-frontend: # Name of the frontend service
    build: frontend/book_arbitrage
    ports: 
      - "10015:80"
    
  book-arbitrage-backend: # Name of the backend service
    build: backend
    ports:
      - "8015:8000"
